apiVersion: v1
kind: Pod
metadata:
  name: pod-name
  labels:
    key-label1: value-label1
    key-label2: value-label2
spec:
  containers:
    - name: container-name
      image: image-name
      ports:
        - containerPort: 80
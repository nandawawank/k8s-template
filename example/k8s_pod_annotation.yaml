apiVersion: v1
kind: Pod
metadata:
  name: nginx-finance
  labels:
    team: finance
    version: 1.0.0
    environment: production
  annotations:
    description: "This is the nginx pod"
spec:
  containers:
    - name: nginx
      image: nginx:1.20
      ports:
        - containerPort: 80